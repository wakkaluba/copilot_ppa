// Minimal type declaration for JS module to satisfy TypeScript
export class ReviewChecklist {
  constructor(context: unknown);
  getAvailableChecklists(): unknown[];
  getChecklistByName(name: string): unknown;
}
